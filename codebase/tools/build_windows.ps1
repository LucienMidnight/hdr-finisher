$ErrorActionPreference = "Stop"
Write-Host "Building HDR Finisher Windows package scaffold"
Write-Host "Install dependencies, verify encoder binaries, then package with PyInstaller in a later milestone."
